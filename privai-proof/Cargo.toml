[package]
name = "privai-proof"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Proof-carrying block verification scaffold for privAI"

[dependencies]
blake3 = "1.5"
privai-chain = { path = "../privai-chain" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
