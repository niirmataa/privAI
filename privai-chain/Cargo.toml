[package]
name = "privai-chain"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
license = "MIT"
description = "Core chain primitives, canonical formats, and consensus objects for privAI"

[dependencies]
blake3 = "1.5"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
